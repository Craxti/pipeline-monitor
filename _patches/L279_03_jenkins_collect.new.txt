                if "allure_max_builds" in inst:
                    allure_max_builds = int(inst.get("allure_max_builds") or 0)
                else:
                    allure_max_builds = 3
                allure_parser = JenkinsAllureParser(
                    url=inst["url"],
                    username=inst.get("username", ""),
                    token=inst.get("token", ""),
                    jobs=jobs_for_allure,
                    max_builds=allure_max_builds,
                    workers=int(inst.get("allure_workers", 6) or 6),
                    timeout=int(inst.get("allure_timeout", 20) or 20),
                    verify_ssl=bool(inst.get("verify_ssl", True)),
                    progress_cb=lambda msg: progress(
                        "jenkins_allure",
                        f"Jenkins: {label}",
                        msg,
                    ),
                    retries=int(inst.get("allure_retries", 2) or 2),
                    backoff_seconds=float(inst.get("allure_backoff_seconds", 0.8) or 0.8),
                    per_task_timeout_sec=int(inst.get("allure_task_timeout_sec", 90) or 90),