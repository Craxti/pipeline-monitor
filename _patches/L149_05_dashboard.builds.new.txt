      _lastBuildsPageSig = pageSig;
      swapTableContentSmooth(tbody, () => { tbody.innerHTML = html; });
      cacheStaleTableHtml('builds', tbody);
    } else tbody.insertAdjacentHTML('beforeend', html);